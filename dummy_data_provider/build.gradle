apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project (":common_data")
    compile group: 'javax.inject', name: 'javax.inject', version: '1'
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
